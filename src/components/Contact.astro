---
---

<section id="contact" class="py-20 bg-dark/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-display font-bold mb-4 fade-in">
        <span class="gradient-text">Contactez</span>-nous
      </h2>
      <p class="text-xl text-gray-400 max-w-3xl mx-auto fade-in">
        Vous souhaitez en savoir plus sur IFAT ? N'hésitez pas à nous contacter.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
      <div class="space-y-8 fade-in">
        <div>
          <h3 class="text-xl font-semibold mb-4">Informations de contact</h3>
          <ul class="space-y-4 text-gray-400">
            <li class="flex items-center">
              <span class="mr-2">📍</span>
              Djibouti, République de Djibouti
            </li>
            <li class="flex items-center">
              <span class="mr-2">📧</span>
              contact@ifat.dj
            </li>
            <li class="flex items-center">
              <span class="mr-2">📱</span>
              +33 07 75 84 09 62
              +221 77 219 82 61
            
            </li>
          </ul>
        </div>

        <div>
          <h3 class="text-xl font-semibold mb-4">Suivez-nous</h3>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-400 hover:text-white transition-colors">
              LinkedIn
            </a>
            <a href="#" class="text-gray-400 hover:text-white transition-colors">
              Twitter
            </a>
          </div>
        </div>
      </div>

      <form class="space-y-6 fade-in">
        <div>
          <label for="name" class="block text-sm font-medium text-gray-400 mb-2">Nom complet</label>
          <input
            type="text"
            id="name"
            name="name"
            class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 focus:border-primary focus:ring-1 focus:ring-primary transition-colors"
          />
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-gray-400 mb-2">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 focus:border-primary focus:ring-1 focus:ring-primary transition-colors"
          />
        </div>

        <div>
          <label for="message" class="block text-sm font-medium text-gray-400 mb-2">Message</label>
          <textarea
            id="message"
            name="message"
            rows="4"
            class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 focus:border-primary focus:ring-1 focus:ring-primary transition-colors"
          ></textarea>
        </div>

        <button
          type="submit"
          class="w-full px-8 py-3 rounded-lg bg-gradient-to-r from-primary to-secondary text-white font-semibold hover:opacity-90 transition-opacity"
        >
          Envoyer le message
        </button>
      </form>
    </div>
  </div>
</section>